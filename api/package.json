{
  "name": "azure-news-api",
  "version": "1.0.0",
  "description": "Azure Static Web Apps API for Cosmos DB news data",
  "main": "articles.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prestart": "npm run build",
    "start": "func start",
    "clean": "rimraf dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "@azure/identity": "^4.0.0"
  },
  "devDependencies": {
    "@azure/functions": "^4.11.0",
    "@types/node": "^20.0.0",
    "azure-functions-core-tools": "^4.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}