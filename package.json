{
  "name": "rapid-azure-digest",
  "version": "1.0.0",
  "description": "Azure Daily News Digest - RSS aggregation and AI summarization",
  "private": true,
  "workspaces": [
    "api",
    "scripts"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=api",
    "dev": "./dev.sh",
    "update-news": "npm run update-news --workspace=scripts",
    "install-all": "npm install --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "help": "echo '🚀 Quick Commands:' && echo '  npm run dev     - Start development server' && echo '  npm run build   - Build all projects' && echo '  npm run update-news - Update RSS news' && echo '' && echo '📖 Documentation:' && echo '  README.md - Full documentation' && echo '  LOCAL_DEVELOPMENT.md - Quick reference' && echo '' && echo '⚠️  Node.js v20 required! Run: export PATH=\"/usr/local/opt/node@20/bin:$PATH\"'",
    "check-node": "node --version && echo 'Node.js version check completed. Required: v20.x.x'",
    "postinstall-disabled": "npm run install-all"
  },
  "keywords": ["azure", "rss", "news", "ai", "summary"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
